
Add some simulation code, alongside the submit button.
This should step through the entire state, with a `currentExecution` struct containing the current state, alongside timing.

The player should be able to visualize stuff as it updates, and there should be a cool "wiping" animation maybe.